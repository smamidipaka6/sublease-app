import React from "react";

const Map = () => {
  const appStyle = {
    width: "100vw",
    height: "100vh",
    background: "url(/Map_Screen.jpeg) no-repeat center center fixed",
    backgroundSize: "100% 100%", // Stretch the image to cover the container
  };

  return <div style={appStyle}></div>;
};

export default Map;
